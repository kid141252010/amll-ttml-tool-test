# 后端安全薄弱点修复清单

**项目**: AMLL TTML Tool  
**分析日期**: 2026-07-07  
**风险等级**: 🔴 高危 | 🟡 中危 | 🟢 低危

---

## 一、认证与授权问题

### 🔴 1.1 敏感 Token 存储在 LocalStorage

**位置**:
- `src/modules/settings/states/index.ts:75-76` - GitHub PAT
- `src/modules/settings/states/index.ts:92-96` - Netease Cookie
- `src/components/OAuthCallbackHandler.tsx:91` - 歌词站 Access Token

**问题描述**:
所有敏感令牌（GitHub Personal Access Token、网易云 Cookie、OAuth Token）都使用 `atomWithStorage` 存储在浏览器的 localStorage 中，容易受到 XSS 攻击窃取。

**风险影响**:
- XSS 攻击可直接读取所有 Token
- 无过期机制，Token 长期有效
- 跨标签页共享，扩大攻击面

**修复建议**:
```typescript
// 1. 使用 httpOnly Cookie（需要后端支持）
// 2. 使用加密存储
// 3. 实现 Token 自动刷新机制
// 4. 添加 Token 过期时间验证

// 示例：添加加密层
import CryptoJS from 'crypto-js';

const encryptToken = (token: string, key: string) => {
  return CryptoJS.AES.encrypt(token, key).toString();
};

const decryptToken = (encrypted: string, key: string) => {
  const bytes = CryptoJS.AES.decrypt(encrypted, key);
  return bytes.toString(CryptoJS.enc.Utf8);
};
```

---

### 🔴 1.2 OAuth PKCE 参数存储在 SessionStorage

**位置**: `src/components/OAuthCallbackHandler.tsx:51-52`

**问题代码**:
```typescript
const storedState = sessionStorage.getItem("lyrics_site_state");
const codeVerifier = sessionStorage.getItem("lyrics_site_code_verifier");
```

**问题描述**:
PKCE 的 `code_verifier` 和 `state` 存储在 sessionStorage 中，可被同源脚本访问。

**修复建议**:
```typescript
// 使用内存存储或加密的 sessionStorage
const secureStorage = {
  set(key: string, value: string) {
    const encrypted = encryptToken(value, window.crypto.randomUUID());
    sessionStorage.setItem(key, encrypted);
  },
  get(key: string) {
    const encrypted = sessionStorage.getItem(key);
    return encrypted ? decryptToken(encrypted, key) : null;
  }
};
```

---

### 🟡 1.3 缺少 Token 有效性验证

**位置**: 
- `src/modules/github/services/identity-service.ts:33-65`
- `src/modules/review/services/remote-service.ts:100-122`

**问题描述**:
应用启动时不验证已存储 Token 的有效性，可能使用已过期或被撤销的 Token。

**修复建议**:
```typescript
// 添加启动时 Token 验证
useEffect(() => {
  const validateStoredToken = async () => {
    if (token) {
      const result = await verifyTokenValidity(token);
      if (!result.valid) {
        setToken('');
        setUser(null);
        showNotification('Token 已失效，请重新登录');
      }
    }
  };
  validateStoredToken();
}, []);
```

---

## 二、API 安全问题

### 🔴 2.1 代理端点缺少速率限制

**位置**:
- `api/github.ts:52-100` - GitHub 代理
- `vite.config.ts:78-131` - 开发环境代理

**问题描述**:
GitHub API 代理和元数据网络代理没有速率限制，可能被滥用为 DDoS 工具或 API 配额耗尽攻击。

**风险影响**:
- 攻击者可通过代理大量请求 GitHub API
- 耗尽 GitHub API 配额
- 造成服务拒绝

**修复建议**:
```typescript
// 添加速率限制中间件
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15分钟
  max: 100, // 限制100个请求
  message: '请求过于频繁，请稍后再试'
});

// 应用到代理路由
app.use('/api/github', limiter);
app.use('/api/metadata-network', limiter);
```

---

### 🔴 2.2 URL 验证不完整

**位置**: `src/modules/review/services/remote-service.ts:16-29`

**问题代码**:
```typescript
const getSafeUrl = (input: string, requireTtml: boolean) => {
  if (!input || /\s/.test(input)) return null;
  try {
    const url = new URL(input);
    if (!["http:", "https:"].includes(url.protocol)) return null;
    if (url.username || url.password) return null;
    // ⚠️ 缺少对 localhost、内网 IP 的检查
    if (requireTtml) {
      const path = url.pathname.toLowerCase();
      if (!path.endsWith(".ttml")) return null;
    }
    return url;
  } catch {
    return null;
  }
};
```

**问题描述**:
- 未阻止 localhost 和内网 IP（SSRF 风险）
- 未检查 DNS 重绑定攻击
- 允许访问 `http://` 协议

**修复建议**:
```typescript
const BLOCKED_HOSTS = new Set([
  'localhost',
  '127.0.0.1',
  '0.0.0.0',
  '::1',
  '[::1]'
]);

const isPrivateIP = (hostname: string): boolean => {
  // 检查私有 IP 范围
  const privateRanges = [
    /^10\./,
    /^172\.(1[6-9]|2\d|3[01])\./,
    /^192\.168\./,
    /^169\.254\./,
    /^fc00:/i,
    /^fe80:/i
  ];
  return privateRanges.some(regex => regex.test(hostname));
};

const getSafeUrl = (input: string, requireTtml: boolean) => {
  if (!input || /\s/.test(input)) return null;
  try {
    const url = new URL(input);
    
    // 仅允许 HTTPS
    if (url.protocol !== 'https:') return null;
    
    // 阻止内网地址
    if (BLOCKED_HOSTS.has(url.hostname.toLowerCase()) || isPrivateIP(url.hostname)) {
      return null;
    }
    
    if (url.username || url.password) return null;
    
    if (requireTtml) {
      const path = url.pathname.toLowerCase();
      if (!path.endsWith(".ttml")) return null;
    }
    return url;
  } catch {
    return null;
  }
};
```

---

### 🟡 2.3 GitHub 代理白名单可被绕过

**位置**: `api/github.ts:2-6`

**问题代码**:
```typescript
const ALLOWED_HOSTS = new Set([
  "api.github.com",
  "github.com",
  "raw.githubusercontent.com",
]);
```

**问题描述**:
仅验证 hostname，不验证完整 URL，可能存在子域名劫持风险。

**修复建议**:
```typescript
const ALLOWED_HOSTS = new Set([
  "api.github.com",
  "github.com",
  "raw.githubusercontent.com",
]);

// 严格验证，不允许子域名
const isAllowedHost = (hostname: string): boolean => {
  return ALLOWED_HOSTS.has(hostname.toLowerCase());
};

const buildTargetFromUrl = (rawUrl: string) => {
  try {
    const url = new URL(rawUrl);
    if (!isAllowedHost(url.hostname)) {
      return null;
    }
    return url;
  } catch {
    return null;
  }
};
```

---

### 🟡 2.4 元数据网络代理请求体大小限制不足

**位置**: `vite.config.ts:302`

**问题代码**:
```typescript
const METADATA_NETWORK_MAX_BODY_BYTES = 64 * 1024; // 64KB
```

**问题描述**:
64KB 的请求体限制可能被用于发送恶意载荷或造成内存占用。

**修复建议**:
```typescript
// 降低到合理大小
const METADATA_NETWORK_MAX_BODY_BYTES = 8 * 1024; // 8KB

// 添加额外验证
if (request.body) {
  const bodySize = Buffer.byteLength(request.body, "utf-8");
  if (bodySize > METADATA_NETWORK_MAX_BODY_BYTES) {
    return { ok: false, error: "Request body too large" };
  }
  
  // 验证 JSON 深度，防止原型污染
  const MAX_DEPTH = 10;
  const checkDepth = (obj: any, depth = 0): boolean => {
    if (depth > MAX_DEPTH) return false;
    if (typeof obj === 'object' && obj !== null) {
      return Object.values(obj).every(val => checkDepth(val, depth + 1));
    }
    return true;
  };
  
  try {
    const parsed = JSON.parse(request.body);
    if (!checkDepth(parsed)) {
      return { ok: false, error: "Request structure too deep" };
    }
  } catch {
    // 非 JSON 也允许
  }
}
```

---

## 三、XSS 防护问题

### 🔴 3.1 TTML 解析器使用 innerHTML

**位置**: `src/modules/project/logic/ttml-parser.ts:1152`

**问题代码**:
```typescript
line.translatedLyricByLang[transLang] = {
  data: wordEl.innerHTML,  // ⚠️ 直接使用 innerHTML
  isAutoFilled,
};
```

**问题描述**:
直接从 XML 中提取 innerHTML 并存储，如果后续渲染时未正确转义，会造成 XSS。

**风险场景**:
恶意 TTML 文件：
```xml
<span ttm:role="x-translation">
  <script>alert('XSS')</script>
  <img src=x onerror="alert('XSS')">
</span>
```

**修复建议**:
```typescript
// 使用 textContent 替代 innerHTML
line.translatedLyricByLang[transLang] = {
  data: wordEl.textContent || '',  // 仅提取文本
  isAutoFilled,
};

// 或使用 DOMPurify 清理
import DOMPurify from 'dompurify';

line.translatedLyricByLang[transLang] = {
  data: DOMPurify.sanitize(wordEl.innerHTML, {
    ALLOWED_TAGS: [],
    ALLOWED_ATTR: []
  }),
  isAutoFilled,
};
```

---

### 🟡 3.2 缺少 CSP（内容安全策略）

**位置**: `src-tauri/tauri.conf.json:27`

**问题代码**:
```json
"security": {
  "csp": null,  // ⚠️ CSP 被禁用
  "headers": {
    "Cross-Origin-Opener-Policy": "same-origin",
    "Cross-Origin-Embedder-Policy": "require-corp"
  }
}
```

**问题描述**:
CSP 被设置为 null，禁用了浏览器的 XSS 防护机制。

**修复建议**:
```json
"security": {
  "csp": "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self' https://api.github.com https://amlldb.bikonoo.com https://ncmapi.bikonoo.com; worker-src 'self' blob:;",
  "headers": {
    "Cross-Origin-Opener-Policy": "same-origin",
    "Cross-Origin-Embedder-Policy": "require-corp",
    "X-Content-Type-Options": "nosniff",
    "X-Frame-Options": "DENY",
    "Referrer-Policy": "strict-origin-when-cross-origin"
  }
}
```

---

## 四、数据验证问题

### 🟡 4.1 缺少输入长度限制

**位置**: `src/modules/lrclib/api/client.ts:12-27`

**问题代码**:
```typescript
async search(query: string): Promise<LrcLibTrack[]> {
  if (!query.trim()) return [];
  
  // ⚠️ 未限制 query 长度
  const response = await fetch(
    `${BASE_URL}/search?q=${encodeURIComponent(query)}`,
  );
```

**修复建议**:
```typescript
async search(query: string): Promise<LrcLibTrack[]> {
  const trimmed = query.trim();
  if (!trimmed) return [];
  
  // 限制查询长度
  const MAX_QUERY_LENGTH = 200;
  if (trimmed.length > MAX_QUERY_LENGTH) {
    throw new Error(`Search query too long (max ${MAX_QUERY_LENGTH} characters)`);
  }
  
  const response = await fetch(
    `${BASE_URL}/search?q=${encodeURIComponent(trimmed)}`,
  );
```

---

### 🟡 4.2 网易云 API 请求缺少超时

**位置**: `src/modules/ncm/services/index.ts:35-39`

**问题代码**:
```typescript
const res = await fetch(url.toString(), {
  method: options.method || "GET",
  credentials: "include",
  // ⚠️ 缺少 timeout
});
```

**修复建议**:
```typescript
const controller = new AbortController();
const timeout = setTimeout(() => controller.abort(), 10000); // 10秒超时

try {
  const res = await fetch(url.toString(), {
    method: options.method || "GET",
    credentials: "include",
    signal: controller.signal
  });
  clearTimeout(timeout);
  // ... 后续处理
} catch (error) {
  clearTimeout(timeout);
  if (error.name === 'AbortError') {
    throw new Error('Request timeout');
  }
  throw error;
}
```

---

## 五、错误处理和信息泄露

### 🟡 5.1 详细错误信息泄露

**位置**: 
- `api/github.ts:98` 
- `vite.config.ts:128`

**问题代码**:
```typescript
catch (error) {
  res.status(502);
  res.send(error instanceof Error ? error.message : "Proxy error");
  // ⚠️ 直接暴露错误信息
}
```

**修复建议**:
```typescript
catch (error) {
  console.error('[GitHub Proxy Error]', error);
  res.status(502);
  // 返回通用错误信息
  res.send(JSON.stringify({
    error: 'Service temporarily unavailable',
    code: 'PROXY_ERROR'
  }));
}
```

---

### 🟡 5.2 敏感信息记录到控制台

**位置**: `src/modules/project/logic/ttml-parser.ts:1363`

**问题代码**:
```typescript
console.log("[TTML Parser] Parsed TTML object:", result);
// ⚠️ 可能包含敏感元数据
```

**修复建议**:
```typescript
// 仅在开发环境输出，且过滤敏感信息
if (import.meta.env.DEV) {
  const sanitized = {
    ...result,
    metadata: result.metadata.map(m => ({
      ...m,
      value: m.key.includes('token') || m.key.includes('key') 
        ? ['***REDACTED***'] 
        : m.value
    }))
  };
  console.log("[TTML Parser] Parsed TTML object:", sanitized);
}
```

---

## 六、依赖和配置问题

### 🔴 6.1 Tauri 开发工具在生产环境启用

**位置**: `src-tauri/tauri.conf.json:20`

**问题代码**:
```json
"devtools": true,  // ⚠️ 生产环境也启用
```

**修复建议**:
```json
"devtools": false,
// 或使用条件配置
// "devtools": process.env.NODE_ENV === 'development'
```

---

### 🟡 6.2 CORS 配置过于宽松

**位置**: `vite.config.ts:419-422`

**当前配置**:
```typescript
headers: {
  "Cross-Origin-Embedder-Policy": "require-corp",
  "Cross-Origin-Opener-Policy": "same-origin",
},
```

**修复建议**:
```typescript
headers: {
  "Cross-Origin-Embedder-Policy": "require-corp",
  "Cross-Origin-Opener-Policy": "same-origin",
  "Access-Control-Allow-Origin": window.location.origin, // 限制为自身域名
  "Access-Control-Allow-Credentials": "true",
  "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
  "Access-Control-Max-Age": "86400"
},
```

---

## 七、会话管理问题

### 🟡 7.1 自动登录失败计数可被绕过

**位置**: `src/modules/ncm/services/auth-service.ts:8-43`

**问题代码**:
```typescript
const readAutoLoginFailures = () => {
  // ⚠️ 使用 localStorage，客户端可随意修改
  if (typeof localStorage === "undefined") {
    return 0;
  }
  try {
    const raw = localStorage.getItem(autoLoginFailureKey);
    // ...
  } catch {
    return 0;
  }
};
```

**修复建议**:
```typescript
// 使用加密存储或服务器端会话
import CryptoJS from 'crypto-js';

const STORAGE_KEY = 'ncm_auth_failures';
const SECRET = window.crypto.randomUUID(); // 或从安全源获取

const readAutoLoginFailures = () => {
  if (typeof localStorage === "undefined") return 0;
  
  try {
    const encrypted = localStorage.getItem(STORAGE_KEY);
    if (!encrypted) return 0;
    
    const decrypted = CryptoJS.AES.decrypt(encrypted, SECRET).toString(CryptoJS.enc.Utf8);
    const data = JSON.parse(decrypted);
    
    // 验证时间戳，防止重放
    if (Date.now() - data.timestamp > 24 * 60 * 60 * 1000) {
      return 0;
    }
    
    return data.count;
  } catch {
    return 0;
  }
};
```

---

## 修复优先级矩阵

| 编号 | 问题 | 风险等级 | 难度 | 优先级 |
|-----|------|---------|------|--------|
| 1.1 | Token 存储在 LocalStorage | 🔴 高危 | 高 | P0 |
| 2.1 | 代理端点缺少速率限制 | 🔴 高危 | 中 | P0 |
| 2.2 | URL 验证不完整（SSRF） | 🔴 高危 | 低 | P0 |
| 3.1 | TTML 解析器使用 innerHTML | 🔴 高危 | 低 | P0 |
| 6.1 | 生产环境启用开发工具 | 🔴 高危 | 低 | P0 |
| 1.2 | OAuth PKCE 参数存储不安全 | 🔴 高危 | 中 | P1 |
| 3.2 | 缺少 CSP | 🟡 中危 | 低 | P1 |
| 1.3 | 缺少 Token 有效性验证 | 🟡 中危 | 中 | P1 |
| 2.3 | GitHub 代理白名单可被绕过 | 🟡 中危 | 低 | P2 |
| 2.4 | 请求体大小限制不足 | 🟡 中危 | 低 | P2 |
| 4.1 | 缺少输入长度限制 | 🟡 中危 | 低 | P2 |
| 4.2 | 请求缺少超时 | 🟡 中危 | 低 | P2 |
| 5.1 | 详细错误信息泄露 | 🟡 中危 | 低 | P3 |
| 5.2 | 敏感信息记录到控制台 | 🟡 中危 | 低 | P3 |
| 6.2 | CORS 配置过于宽松 | 🟡 中危 | 低 | P3 |
| 7.1 | 自动登录失败计数可被绕过 | 🟡 中危 | 中 | P3 |

---

## 快速修复脚本

创建 `security-fixes.sh`：

```bash
#!/bin/bash

echo "🔒 开始应用安全修复..."

# 1. 安装安全依赖
pnpm add crypto-js dompurify express-rate-limit

# 2. 禁用生产环境开发工具
sed -i 's/"devtools": true/"devtools": false/' src-tauri/tauri.conf.json

# 3. 添加 CSP
# (需要手动编辑 tauri.conf.json)

# 4. 创建安全工具模块
mkdir -p src/utils/security
cat > src/utils/security/token-storage.ts << 'EOF'
import CryptoJS from 'crypto-js';

const SECRET_KEY = window.crypto.randomUUID();

export const secureStorage = {
  set(key: string, value: string): void {
    const encrypted = CryptoJS.AES.encrypt(value, SECRET_KEY).toString();
    localStorage.setItem(key, encrypted);
  },
  
  get(key: string): string | null {
    const encrypted = localStorage.getItem(key);
    if (!encrypted) return null;
    
    try {
      const bytes = CryptoJS.AES.decrypt(encrypted, SECRET_KEY);
      return bytes.toString(CryptoJS.enc.Utf8);
    } catch {
      return null;
    }
  },
  
  remove(key: string): void {
    localStorage.removeItem(key);
  }
};
EOF

echo "✅ 安全修复脚本执行完成"
echo "⚠️  请手动检查并应用 CSP 配置"
```

---

## 安全检查清单

在部署前检查：

- [ ] 所有 Token 使用加密存储
- [ ] API 代理启用速率限制
- [ ] URL 验证阻止内网访问
- [ ] CSP 已启用并正确配置
- [ ] 生产环境禁用 devtools
- [ ] 所有用户输入经过验证和清理
- [ ] 错误信息不泄露内部细节
- [ ] HTTPS 强制启用
- [ ] 依赖项无已知漏洞（运行 `pnpm audit`）

---

## 联系与支持

如有安全问题，请通过以下方式报告：
- 项目 Issue（非紧急）
- 安全邮箱（紧急漏洞）

**最后更新**: 2026-07-07
